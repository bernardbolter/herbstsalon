import React from 'react'
import * as styles from '../styles/nav.module.scss'

const Nav = ({ location }) => {
    return (
        <nav className={styles.container}>
            <p>Nav</p>
        </nav>
    )
}

export default Nav